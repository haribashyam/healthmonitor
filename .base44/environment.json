{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "GEMINI_API_KEY",
      "description": "Google Gemini API key for server-side AI endpoints (optional; app falls back to deterministic mock AI without it). Get at https://aistudio.google.com/apikey.",
      "requiredAtBoot": false
    }
  ]
}
